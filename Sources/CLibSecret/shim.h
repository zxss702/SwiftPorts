// Umbrella header for the system libsecret library. The include path
// (and glib/gobject/gio transitive includes) are supplied by
// `pkg-config libsecret-1`, wired via the `CLibSecret` systemLibrary
// target's `pkgConfig:` in Package.swift.
#include <libsecret/secret.h>
